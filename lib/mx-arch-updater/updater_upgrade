#!/bin/sh
set -eu
MODE="basic"
if [ "${1:-}" = "--mode" ] && [ -n "${2:-}" ]; then
  MODE="$2"
fi
case "$MODE" in
  basic|full)
    exec pacman -Syu
    ;;
  *)
    echo "Unknown upgrade mode: $MODE" >&2
    exit 2
    ;;
esac
